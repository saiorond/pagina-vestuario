const email = document.getElementById("email").value;

email.addEventListener('submit', (e) => {
    e.preventDefault();

    checarInputs();
});

function checarInputs() {
    if (email == '') {
        
    }
}